"use client";

/*
 File Header (auto-generated by scripts/add-file-headers.ts)
 - File: app/(main)/dashboard/loans/page.tsx
 - Purpose: Next.js page for /dashboard/loans.
 - Context: Participates in the app router hierarchy for end-user navigation.
 - How it works: Defines a React component rendered by the app router; fetches and displays data via services/hooks. Uses: React Client Component.
 - External imports: react, lucide-react
 - Local imports: @/components/data-table/simple-data-table, @/components/ui/button, @/components/ui/card, ./_components/columns, ./_components/loan-detail-drawer, ./_components/loan-wizard, @/types/loan
 - Refactoring ideas: Split complex components into smaller units. | Memoize expensive subtrees and handlers. | Extract data fetching into hooks/services.
 - Security considerations: Avoid dangerouslySetInnerHTML; sanitize any HTML content. | Do not embed secrets or tokens in client code. | Harden forms against CSRF where applicable.
*/
import { useEffect, useState } from "react";
import { Plus } from "lucide-react";
import { SimpleDataTable as DataTable } from "@/components/data-table/simple-data-table";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { createColumns } from "./_components/columns";
import { LoanDetailDrawer } from "./_components/loan-detail-drawer";
import { LoanWizard } from "./_components/loan-wizard";
import type { Loan } from "@/types/loan";

export default function LoansPage() {
  const [loans, setLoans] = useState<Loan[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [selectedLoanId, setSelectedLoanId] = useState<string | null>(null);
  const [isDrawerOpen, setIsDrawerOpen] = useState(false);
  const [isWizardOpen, setIsWizardOpen] = useState(false);

  useEffect(() => {
    fetchLoans();
  }, []);

  const fetchLoans = async () => {
    try {
      const response = await fetch("/api/v1/loans");
      const result = await response.json();

      if (result.success) {
        setLoans(result.data);
      }
    } catch (error) {
      console.error("Error fetching loans:", error);
    } finally {
      setIsLoading(false);
    }
  };

  const handleViewDetails = (loanId: string) => {
    setSelectedLoanId(loanId);
    setIsDrawerOpen(true);
  };

  const columns = createColumns(handleViewDetails);

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-3xl font-bold">Loans</h1>
          <p className="text-muted-foreground">Manage your loan portfolio</p>
        </div>
        <Button onClick={() => setIsWizardOpen(true)}>
          <Plus className="mr-2 size-4" />
          New Loan
        </Button>
      </div>

      <Card>
        <CardHeader>
          <CardTitle>All Loans</CardTitle>
          <CardDescription>View and manage all loans in your portfolio</CardDescription>
        </CardHeader>
        <CardContent>
          {isLoading ? (
            <div className="flex h-32 items-center justify-center">
              <p className="text-muted-foreground">Loading...</p>
            </div>
          ) : (
            <DataTable columns={columns} data={loans} />
          )}
        </CardContent>
      </Card>

      <LoanDetailDrawer
        loanId={selectedLoanId}
        open={isDrawerOpen}
        onOpenChange={setIsDrawerOpen}
        onUpdate={fetchLoans}
      />

      <LoanWizard
        open={isWizardOpen}
        onOpenChange={setIsWizardOpen}
        onComplete={fetchLoans}
      />
    </div>
  );
}

