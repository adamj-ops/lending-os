// Archived helper. Prefer using `npm run db:push` with drizzle-kit.
// Kept for reference only.

import { config } from 'dotenv';
config({ path: '.env.local' });

console.log('Please use: npm run db:push');

